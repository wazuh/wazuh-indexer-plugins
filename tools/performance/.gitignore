# Run outputs (metrics, reports)
runs/

# Staged custom indexer package (run.sh --package)
.pkg/

# Analyze outputs written to the tool dir (analyze.sh + compare.py / plot.py defaults)
*compare.md
*timeline.png
*averages.png

# Rendered monitor scrape config (produced by setup-monitor.sh)
monitoring/prometheus.rendered.yml

# Python virtual env and other local files
venv/
.venv/
__pycache__/
