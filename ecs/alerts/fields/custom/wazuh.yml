---
- name: wazuh
  title: Wazuh
  description: >
    Wazuh Inc. custom fields
  fields:
    - name: decoder
      type: keyword
      level: custom
      description: >
        Wazuh decoder used.
      normalize:
        - array
      example: ["foo", "bar"]
    - name: rules
      type: keyword
      level: custom
      description: >
        Wazuh rules triggered by the event.
      normalize:
        - array
      example: ["foo", "bar"]
