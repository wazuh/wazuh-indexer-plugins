---
- name: network
  title: Network
  type: group
  group: 2
  description: >
    Network related data.
  fields:
    - name: broadcast
      type: ip
      level: custom
      description: >
        Broadcast address
    - name: dhcp
      type: keyword
      level: custom
      description: >
        DHCP status (enabled, disabled, unknown, BOOTP)
    - name: gateway
      type: ip
      level: custom
      description: >
        Gateway address
    - name: metric
      type: long
      level: custom
      description: >
        Metric of the network protocol
    - name: netmask
      type: ip
      level: custom
      description: >
        Network mask
