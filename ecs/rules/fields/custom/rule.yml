---
- name: rule
  title: Wazuh Rules
  short: Wazuh Inc. custom fields.
  description: >
    Wazuh rules fields.
  type: group
  group: 2
  fields:
    - name: id
      type: integer
      level: custom
      description: >
        Unique identifier of the rule.
    - name: name
      type: keyword
      level: custom
      description: >
        The rule's name.
    - name: body
      type: object
      level: custom
      description: >
        The rule's body, containing rule logic such as FIND, MATCH, or exact matching conditions.
    - name: created_at
      type: date
      level: custom
      description: >
        The rule's creation date.
