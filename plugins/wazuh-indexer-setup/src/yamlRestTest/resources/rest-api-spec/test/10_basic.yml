---
"Plugin load test":
  - do:
      cat.plugins:
        local: true
        h: component

  - match:
      $body: /^wazuh-indexer-setup\n$/

---
"Check index is created":
  - do:
      cat.indices: {}

  - match: { $body: /.+wazuh-indexer-setup-plugin.+/ }

