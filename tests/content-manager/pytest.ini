[pytest]
addopts = -ra
filterwarnings =
    ignore::urllib3.exceptions.InsecureRequestWarning
markers =
    smoke: minimal happy-path checks
    crud: resource create/update/delete coverage
    policy: policy update coverage
    promote: promotion lifecycle coverage (mutates test/custom spaces irreversibly)
    logtest: engine-backed logtest coverage
